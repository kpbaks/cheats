% fish, shell, builtin

# List the names of all builtin functions/commands in fish
builtin --names

# Test if any of the specified builtins exists in fish
buitin --query <builtins>

# Check if last command succeeded in fish
echo $status

$ builtins: builtin --names
